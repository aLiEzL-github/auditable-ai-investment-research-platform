# compose.yaml —— G0-07 验收地（本机 arm64 实测通过；amd64 验收移至 GitHub runner）
# 数据禁入：无任何 volume 挂载数据目录（L1 三层分离）
# G-4/F1：端口仅本机发布（127.0.0.1），不与 main.py 的「本机安全」意图相抵

services:
  app:
    build:
      context: .
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
