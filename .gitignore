# .gitignore —— ADR-006 L4 数据禁入 + Python/Docker 常规排除

# ── 数据禁入（ADR-006 L1/L4）：原始数据与转储一律不得入仓 ──────
# 数据类扩展名
*.pdf
*.xbrl
*.xlsx
*.xls
*.zip
*.doc
*.docx
*.csv
*.tsv
*.jsonl
*.parquet
*.db
*.sqlite
*.sqlite3

# 数据/对象库目录（L1 三层分离：原始字节只进本地对象库）
data/
object-store/
raw/
cache/
downloads/
fixtures/real/

# ── 凭据与秘密 ──────────────────────────────────────────────────
.env
.env.*
!/.env.example  # G1-05 模板（无真实值，须入仓供 secret_scan 扫描）
# G7-01 E2E 构建模式（VITE_API_MODE=http，无秘密，须入仓）——
# 注意：gitignore 的「行尾 #」会并入模式本体，故注释必须独占一行
!/frontend/.env.g7-e2e
*.pem
*.key
id_ed25519*
id_rsa*
secrets/

# ── Python ──────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# ── 工具与系统 ──────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.log
.wheel_policy_cache.json

# ── frontend（G5-01）────────────────────────────────────────────
node_modules/
frontend/node_modules/
frontend/dist/
frontend/dist-g7-e2e/
frontend/*.tsbuildinfo
frontend/test-results/
frontend/playwright-report/

# OI-PF-186：变异注入/临时脚本不得入库。mut_g6path.py 曾被 git add -A
# 扫进仓库根并随公开 main 发布（含本机路径与私有台账目录布局）。
# 守卫 backend/tools/repo_hygiene_check.py 的 H-2 是兜底，本规则是第一道。
/mut_*.py
/_scratch*
/*.tmp.py

# ── 研究产出禁入（OI-PF-022 / VD-20 = 仅内部；ADR-002 三层分离）────────
# 这一层是 G1-01 的交付内容，与 CI 侧的 research_data_interdict.py（G1-07）
# **各管一半**：.gitignore 防误 git add；CI 防 git add -f、防粘进 Markdown。
# ADR-006 L4 原文：「.gitignore 不够」—— 不够不等于不要，两层缺一都留口子。
# 2026-08-17 实测：闭合 OI-PF-022 时才发现这一层从未落地 ——
# 研究产出目录一个也没被忽略，全靠 CI 单层拦。
golden-baselines/
evidence-packs/
candidates/
research/
portfolio/

# ADR-006 L5 指针化（OI-PF-026 统计局条款）：原始序列不入库，
# 只放 locator + SHA-256 + 少量事实数字。
raw-series/
*.xbrl
